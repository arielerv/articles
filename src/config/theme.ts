const theme = {
  size: (n: number) => {
    return `${n * 16}px`;
  },
  colors: {
    white: "#fff",
    dark: "#000"
  },
  fonts: {
    serif: "SuecaSlab Georgia serif",
    bold: "SuecaSlab Georgia Pro Bold",
  },
  sizes: {
    header: "14px"
  }
};
export default theme;
